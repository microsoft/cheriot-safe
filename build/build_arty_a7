#!/usr/bin/sh
DESIGNROOT="../"
NETLISTDIR="RUN_$(date +%s)"
mkdir $NETLISTDIR

rm -f build.passed

vivado -mode batch -source cheri_fpga.tcl -log $NETLISTDIR/vivado.log -tclargs $DESIGNROOT $NETLISTDIR ./read_design.tcl ./arty-cheri-a7-100-eth.xdc

